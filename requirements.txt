feedparser>=6.0.10
PyYAML>=6.0
certifi>=2024.2.2
requests>=2.31.0

# AI 레이어 (llm_provider.py). 미설치여도 수집·필터 단계는 동작.
anthropic>=0.40.0,<1   # 1.x는 messages.create(temperature=…)를 받지 않아 llm_provider 호출이 실패한다(2026-09-21 확인)
# openai>=1.0.0   # OpenAI 어댑터 사용 시 주석 해제
trafilatura
googlenewsdecoder

# 국가별 거시지표(indicators.py) — 주가지수 조회(무료, 키 불필요)
yfinance
